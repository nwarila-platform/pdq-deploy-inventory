# composition build tree (framework checkout + overlaid role) — never committed
.compose/

# ansible scratch
*.retry
*.log

# strict-cycle worktrees live outside the repo (../.worktrees) but guard anyway
.worktrees/

# local-only secrets / scratch (never commit credentials)
*.secret
temp-password*

# Withheld from the public repository (Director, 2026-07-29): these enumerate findings still OPEN
# in secure-wazuh, a public sibling. Kept symmetric with windows-wsus so neither repo can publish
# them by accident.
_handoff/AWS-IAM-AUDIT-wazuh.md
_handoff/WAZUH-IAM-BACKPORT.md
