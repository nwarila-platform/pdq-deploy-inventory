# Changelog

All notable changes to this project are documented here. Format loosely follows
[Keep a Changelog](https://keepachangelog.com/); versions are cut by release-please once wired.

## [Unreleased]

### Added
- Repo skeleton scaffolded (2026-07-27): mirrors `secure-wazuh` structure with `windows-wsus`
  Windows lessons folded in. `pdq` role skeleton (loader, defaults, validate, present/clean
  stubs), reused `windows_disk_manager`, `pdq.yml` play, dev inventory, compose + gate scripts,
  strict-cycle handoff docs, and the PDQ build queue.
