# `pdq_deploy` role

Ground-up rebuild in progress (started 2026-08-12): the role is currently a scaffold — the v3
loader (`tasks/main.yml`) plus stubbed task, defaults, and vars files. Capability documentation
returns as each capability lands.
