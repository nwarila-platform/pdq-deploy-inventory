# Build marker, not the script. scripts/materialize-role-scripts.sh copies the source named
# below to files/Remove-PdqPackage.ps1 before the role is linted or run; that copy is a
# build artifact the .gitignore deliberately never allowlists.
#
# The script is developed, reviewed and pair-tested once under scripts/, beside its
# Remove-PdqPackage.pester.ps1 spec. It is Deploy's alone -- packages are a Deploy concept and
# PDQ Inventory has no equivalent, so no other role names this source.
scripts/Remove-PdqPackage.ps1
